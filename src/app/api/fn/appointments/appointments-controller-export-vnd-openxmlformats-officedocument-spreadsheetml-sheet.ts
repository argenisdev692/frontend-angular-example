/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AppointmentsControllerExport$VndOpenxmlformatsOfficedocumentSpreadsheetmlSheet$Params {
  format?: 'csv' | 'xlsx' | 'pdf';
  statusLead?: 'New' | 'Called' | 'Pending' | 'Declined';
  city?: string;
  state?: string;
  country?: string;
  owner?: string;

/**
 * Include soft-deleted appointments in the export.
 */
  withTrashed?: (boolean | 'true' | 'false');

/**
 * Export ONLY soft-deleted appointments. Cannot be combined with `withTrashed`. Requires `Action.Restore`.
 */
  onlyTrashed?: (boolean | 'true' | 'false');

/**
 * Filter appointments created on or after this date (inclusive). Format: YYYY-MM-DD.
 */
  start_date?: string;

/**
 * Filter appointments created on or before this date (inclusive). Format: YYYY-MM-DD.
 */
  end_date?: string;
}

export function appointmentsControllerExport$VndOpenxmlformatsOfficedocumentSpreadsheetmlSheet(http: HttpClient, rootUrl: string, params?: AppointmentsControllerExport$VndOpenxmlformatsOfficedocumentSpreadsheetmlSheet$Params, context?: HttpContext): Observable<StrictHttpResponse<any>> {
  const rb = new RequestBuilder(rootUrl, appointmentsControllerExport$VndOpenxmlformatsOfficedocumentSpreadsheetmlSheet.PATH, 'get');
  if (params) {
    rb.query('format', params.format, {});
    rb.query('statusLead', params.statusLead, {});
    rb.query('city', params.city, {});
    rb.query('state', params.state, {});
    rb.query('country', params.country, {});
    rb.query('owner', params.owner, {});
    rb.query('withTrashed', params.withTrashed, {});
    rb.query('onlyTrashed', params.onlyTrashed, {});
    rb.query('start_date', params.start_date, {});
    rb.query('end_date', params.end_date, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<any>;
    })
  );
}

appointmentsControllerExport$VndOpenxmlformatsOfficedocumentSpreadsheetmlSheet.PATH = '/api/v1/appointments/export';
