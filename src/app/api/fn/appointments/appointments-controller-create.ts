/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateAppointmentDto } from '../../models/create-appointment-dto';
import { CreateAppointmentResponse } from '../../models/create-appointment-response';

export interface AppointmentsControllerCreate$Params {
      body: CreateAppointmentDto
}

export function appointmentsControllerCreate(http: HttpClient, rootUrl: string, params: AppointmentsControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<CreateAppointmentResponse>> {
  const rb = new RequestBuilder(rootUrl, appointmentsControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CreateAppointmentResponse>;
    })
  );
}

appointmentsControllerCreate.PATH = '/api/v1/appointments';
