/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoleResponseDto } from '../../models/role-response-dto';
import { UpdateRoleDto } from '../../models/update-role-dto';

export interface RolesControllerUpdate$Params {
  id: string;
      body: UpdateRoleDto
}

export function rolesControllerUpdate(http: HttpClient, rootUrl: string, params: RolesControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<RoleResponseDto>> {
  const rb = new RequestBuilder(rootUrl, rolesControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoleResponseDto>;
    })
  );
}

rolesControllerUpdate.PATH = '/api/v1/roles/{id}';
