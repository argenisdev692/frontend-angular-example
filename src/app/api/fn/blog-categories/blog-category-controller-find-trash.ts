/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BlogCategoryResponse } from '../../models/blog-category-response';

export interface BlogCategoryControllerFindTrash$Params {
  skip?: number;
  limit?: number;
}

export function blogCategoryControllerFindTrash(http: HttpClient, rootUrl: string, params?: BlogCategoryControllerFindTrash$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<BlogCategoryResponse>>> {
  const rb = new RequestBuilder(rootUrl, blogCategoryControllerFindTrash.PATH, 'get');
  if (params) {
    rb.query('skip', params.skip, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<BlogCategoryResponse>>;
    })
  );
}

blogCategoryControllerFindTrash.PATH = '/api/v1/blog-categories/trash';
