/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CompanyDataResponse } from '../../models/company-data-response';

export interface CompanyDataControllerDeleteSignature$Params {
  id: string;
}

export function companyDataControllerDeleteSignature(http: HttpClient, rootUrl: string, params: CompanyDataControllerDeleteSignature$Params, context?: HttpContext): Observable<StrictHttpResponse<CompanyDataResponse>> {
  const rb = new RequestBuilder(rootUrl, companyDataControllerDeleteSignature.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CompanyDataResponse>;
    })
  );
}

companyDataControllerDeleteSignature.PATH = '/api/v1/company-data/{id}/signature';
